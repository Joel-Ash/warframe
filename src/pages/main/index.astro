---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import FormattedDate from '../../components/FormattedDate.astro';
import Header from '../../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../../consts';

const posts = (await getCollection('main')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="ko">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    <style>
      main {
        width: 960px;
        margin: 0 auto;
        text-align: center;
      }

      /* ✅ 상단 안내 문구 */
      .tab-notice {
        margin: 3.5rem auto 4.5rem;
        max-width: 820px;
      }

      .tab-notice p {
        margin: 0.6rem 0;
        font-size: 1.21rem;
        font-weight: 700;
        line-height: 1.65;
        color: rgba(180, 200, 255, 0.85);
      }

      ul {
        display: flex;
        flex-wrap: wrap;
        gap: 2rem;
        list-style-type: none;
        margin: 0;
        padding: 0;
      }

      ul li {
        width: calc(50% - 1rem);
      }

      ul li * {
        text-decoration: none;
        transition: 0.2s ease;
      }

      ul li:first-child {
        width: 100%;
        margin-bottom: 1rem;
      }

      ul li:first-child img {
        width: 100%;
      }

      ul li:first-child .title {
        font-size: 2.369rem;
      }

      ul li img {
        margin-bottom: 0.5rem;
        border-radius: 12px;
      }

      ul li a {
        display: block;
      }

      .title {
        margin: 0;
        color: rgba(255, 255, 255, 0.95);
        line-height: 1.15;
        font-weight: 800;
      }

      .date {
        margin: 0.4rem 0 0;
        color: rgba(255, 255, 255, 0.55);
      }

      ul li a:hover h4,
      ul li a:hover .date {
        color: rgb(var(--accent));
      }

      ul a:hover img {
        box-shadow: var(--box-shadow);
      }

      @media (max-width: 720px) {
        main {
          width: 100%;
          padding: 0 1rem;
        }

        ul {
          gap: 0.7rem;
        }

        ul li {
          width: 100%;
        }

        ul li:first-child .title {
          font-size: 1.6rem;
        }

        .tab-notice p {
          font-size: 0.95rem;
        }
      }
    </style>
  </head>

  <body>
    <Header />

    <main>
      <section class="tab-notice">
        <p>
          본 문서는 가독성과 전달력을 우선하여 구어체 및 반말 표현을 사용하고 있습니다.
        </p>
        <p>
          이는 개인 정리 문서의 특성을 반영한 표현 방식이며, 무례함이나 공격적인 의도를 포함하지 않습니다.
        </p>
      </section>

      <section>
        <ul>
          {posts.map((post) => (
            <li>
              <a href={`/main/${post.id}/`}>
                {post.data.heroImage && (
                  <Image width={720} height={360} src={post.data.heroImage} alt="" />
                )}
                <h4 class="title">{post.data.title}</h4>
                <p class="date">
                  <FormattedDate date={post.data.pubDate} />
                </p>
              </a>
            </li>
          ))}
        </ul>
      </section>
    </main>

    <Footer />
  </body>
</html>
